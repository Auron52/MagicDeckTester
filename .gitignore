# Prerequisites
*.d

# Compiled Object files
*.slo
*.lo
*.o
*.obj

# Precompiled Headers
*.gch
*.pch

# Linker files
*.ilk

# Debugger Files
*.pdb

# Compiled Dynamic libraries
*.so
*.dylib
*.dll
*.so.*


# Fortran module files
*.mod
*.smod

# Compiled Static libraries
*.lai
*.la
*.a
*.lib

# Executables
*.exe
*.out
*.app

# Build directories
build/
Build/
build-*/

# CMake generated files
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
Makefile
install_manifest.txt
compile_commands.json

# Temporary files
*.tmp
*.log
*.bak
*.swp

# vcpkg
vcpkg_installed/

# debug information files
*.dwo

# test output & cache
Testing/
.cache/

# simulation logs (keep the dir itself via .gitkeep so it ships in a fresh clone)
logs/*
!logs/.gitkeep
logs_*/

# A/B harness scratch — kept locally, not committed (scripts, result snapshots, built binaries)
ab_runs/

# regression test run output (ground truth regression_gt.txt IS committed; the
# rest is transient: per-mode summaries, per-case logs, and the results/<mode>.env
# fingerprints that 'regression.sh --accept' promotes into the ground truth)
test/regression_result.txt
test/regression_result_*.txt
test/logs/
test/results/
test/ab_scratch/

# Python bytecode cache
__pycache__/
*.pyc
# Exhaustive keep/bottom tables: commit the .gz (small), never the uncompressed blob (~90MB).
# Broad glob catches every uncompressed variant (.profile.json, .raw.json, .adaptive.rN.profile.json);
# the .gz files end in .gz so they are NOT matched, and per-deck <name>.profile.json lacks the
# "keepmodel.exhaustive" infix so analyzer profiles stay tracked.
# decks/**/ reaches into per-deck folders (decks/<name>/<name>.keepmodel.exhaustive.*.json).
decks/**/*.keepmodel.exhaustive*.json
# Per-run generator scratch that now lands next to the deck by default (no flag): the fingerprint-gated
# bucket cache, the crash-recovery journal, and the slow-rollout evidence log. All are derived and
# re-created on demand -- the journal is deleted by a run that finishes.
decks/**/*.keepmodel.gencache.json
decks/**/*.journal
decks/**/*.slow.log
scripts/*.so

# Parsed-sidecar binary cache (derived; rebuilt from the .gz on demand)
*.bincache

# Node dev dependencies for the viewer client check (test/package.json pins jsdom;
# the tree itself is reinstallable with `npm i` in test/, so never tracked).
node_modules/
